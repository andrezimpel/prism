<%= simple_form_for(@gallery) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%#= f.select :client_id, Profile.all.collect { |c| [c.user.fullname, c.id] } %>
    <%= f.hidden_field :account_id, value: 1 %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Save" %>
    <%= link_to 'Cancel', backend_galleries_path, class: "btn btn-danger btn-link" %>
  </div>
<% end %>
